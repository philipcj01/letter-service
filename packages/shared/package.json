{
  "name": "@cloudletters/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit --incremental",
    "lint": "eslint src --no-error-on-unmatched-pattern",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
